from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A project that fetch the youtube comments and then do the sentiment analyis on top of that',
    author='Harsimrandosanjh',
    license='MIT',
)
